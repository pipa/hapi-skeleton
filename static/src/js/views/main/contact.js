// Load Modules =================================
import FormContainer from './components/Form';

// Render tour grid =============================
ReactDOM.render(
    <FormContainer />,
    document.getElementById('contact-form')
);
